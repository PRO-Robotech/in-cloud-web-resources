{{ if not .Values.namespaceNavigation }}
---
apiVersion: front.in-cloud.io/v1alpha1
kind: Breadcrumb
metadata:
  name: stock-cluster-projects-list
spec:
  id: "stock-cluster-projects-list"
  breadcrumbItems:
    - key: api
      label: "{apiGroup}/{apiVersion}/{plural}"
{{ end }}
